# Backend origin the browser calls directly (CORS is enabled on the backend).
# Inlined into the client bundle at build time (NEXT_PUBLIC_*).
# Local dev: http://localhost:8000    Docker: http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
